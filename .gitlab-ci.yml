stages:
  - docker-build

docker-build:
  stage: docker-build
  image: docker:24.0.5
  script:
    - docker info
    - docker build --pull -t rockschool-api:$CI_COMMIT_SHORT_SHA -t rockschool-api:latest -f Dockerfile .
  only:
    - main
    - master